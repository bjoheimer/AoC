# TODO this shall become the file you always execute. 
# It should receive the input x from the user to run x.py.
# Then it should ask which input file to use (if there are multiple), i.e. on user input y, the file inputx_y.txt should be used.
# The purpose of this is code reusability to avoid repetition of the file stream logic. 